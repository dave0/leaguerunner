{include file="common/basic_header.tmpl"}

<h1>{$site.name} ({$site.code})</h1>
<p>
  {if $perm_site_edit}<a href="{$app_cgi_location}?op=site_edit&id={$id}">edit site</a> |{/if}
  {if $perm_field_create}<a href="{$app_cgi_location}?op=field_create&site_id={$id}">add field</a>
  {/if}
</p>

<table border="0" cellpadding="3" cellspacing="0">
    <tr>
      <td class='row_title'>Site Name:</td> 
      <td class='row_data'>{$site.name}</td>
    </tr>
    <tr>
      <td class='row_title'>Site Code:</td> 
      <td class='row_data'>{$site.code}</td>
    </tr>
    <tr>
      <td class='row_title'>Region:</td> 
      <td class='row_data'>{$site.region}</td>
    </tr>
    <tr>
      <td class='row_title' valign='top'>Site Directions:</td> 
      <td class='row_data'>{$site.directions}</td>
    </tr>
    <tr>
      <td class='row_title' valign='top'>Site-specific instructions:</td> 
      <td class='row_data'>{$site.instructions}</td>
    </tr>
    <tr>
      <td class='row_title' valign='top'>Fields:</td> 
      <td class='row_data'>
      <ul>
      {foreach name=fields from=$site.fields item=field}
        <li>{$site.code}{$field.num} ({$field.status})
| <a href="{$app_cgi_location}?op=field_view&id={$field.field_id}">view</a> 
{if $perm_site_edit}
| <a href="{$app_cgi_location}?op=field_edit&id={$field.field_id}">edit</a> 
{/if}
|
      {/foreach}
      </ul>
      </td>
    </tr>
    <tr>
      <td class='row_title' valign='top'>Site Location Map:</td> 
      <td class='row_data' valign='top'>
        {if $site.location_url}
          <a href='{$site.location_url}' target=_new>Click for map in new window</a>
        {else}
          No map
        {/if} 
      </td>
    </tr>
    <tr>
      <td class='row_title' valign='top'>Field Layout Map:</td> 
      <td class='row_data' valign='top'>
        {if $site.layout_url}
          <a href='{$site.layout_url}' target=_new>Click for layout in new window</a>
        {else}
          No map
        {/if} 
      </td>
    </tr>
</table>

<ul>
</ul>


{include file="common/basic_footer.tmpl"}
{* vim: set sw=2 ts=2 et: *}
