{include file="common/basic_header.tmpl"}
<h1>{$page_title}</h1>

<form action="{$app_cgi_location}" method="POST">
	<input type="hidden" name="op" value="{$page_op}">
	<input type="hidden" name="step" value="{$page_step}">
	<input type="hidden" name="id" value="{$id}">
	<input type="hidden" name="target_id" value="{$to_league.id}">
	<input type="hidden" name="team_id" value="{$team_id}">
  
	<blockquote>
        You are attempting to move the team <b>{$team_name|escape}</b> from 
         <b>{$from_league.name}{if $from_league.tier} Tier {$from_league.tier}{/if}</b> to
         <b>{$to_league.name}{if $to_league.tier} Tier {$to_league.tier}{/if}</b>.  If this is correct, please click "Submit" below.
        league you wish to move it to.
	</blockquote>
<br>
<input type="submit" name="submit" value="Submit">
<input type="reset" name="reset" value="Reset">
	</form>
{include file="common/basic_footer.tmpl"}
{* vim: set ai sw=2 ts=2 et: *}
